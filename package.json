{
  "name": "dee-training-site",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro check && astro build",
    "preview": "astro preview",
    "test:links": "npm run build && node scripts/check-campaign-links.mjs && node scripts/check-header-downloads.mjs"
  },
  "dependencies": {
    "@astrojs/check": "^0.9.10",
    "@astrojs/mdx": "^8.0.0",
    "@astrojs/sitemap": "^3.7.4",
    "astro": "^7.3.1",
    "typescript": "^6.0.3"
  }
}
